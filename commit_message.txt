fix: enforce strict boundary sanitization for improved prompt extraction

Add a final sanitization pass in auto_improve_prompt to strip any text
outside <improved_prompt> XML boundaries. This prevents reasoning or
analysis text emitted by the LLM before the tag from leaking into the
final improved prompt.

- Re-extract content if nested or multiple tags confuse the first pass
- Ensure no preceding reasoning or trailing noise survives extraction
- All 19 existing auto-improve tests pass with no regressions
- Added thinking constraint to the improvement prompt